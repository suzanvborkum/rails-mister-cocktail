</div class="container">
    <%= simple_form_for [@cocktail, @review] do |f| %>
      <%= f.input :description %>
      <%= f.input :rating %>
      <%= f.button :submit, class: 'btn btn-danger' %>
    <% end %>
</div>
